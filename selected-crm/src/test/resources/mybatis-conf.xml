<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

    <settings>
        <setting name="cacheEnabled" value="true"/>  <!-- 全局映射器启用缓存 -->
        <setting name="useGeneratedKeys" value="true"/>  <!-- 允许 JDBC 支持自动生成主键 -->
        <setting name="defaultExecutorType" value="REUSE"/>  <!-- 配置默认的执行器 -->
        <setting name="logImpl" value="SLF4J"/>  <!-- 指定 MyBatis 所用日志的具体实现 -->
    </settings>

    <typeAliases>
        <typeAlias type="com.shinetech.crm.domain.CrmClue" alias="CrmClue"/>
        <typeAlias type="com.shinetech.crm.domain.CrmCustomer" alias="CrmCustomer"/>
        <typeAlias type="com.shinetech.crm.domain.CrmOrder" alias="CrmOrder"/>
    </typeAliases>

    <!--PageHelper分页插件配置-->
    <plugins>
        <plugin interceptor="com.github.pagehelper.PageInterceptor">
            <property name="helperDialect" value="mysql"/>
        </plugin>
    </plugins>

    <environments default="development">
        <environment id="development">
            <transactionManager type="MANAGED">
                <property name="closeConnection" value="false"/>
            </transactionManager>
            <dataSource type="POOLED">
                <property name="driver" value="com.mysql.cj.jdbc.Driver"/>
                <property name="url" value="jdbc:mysql://localhost:3307/selected-crm"/>
                <property name="username" value="root"/>
                <property name="password" value="000"/>
            </dataSource>
        </environment>
    </environments>

    <mappers>
        <mapper resource="mapper/CrmClueMapper.xml" />
        <mapper resource="mapper/CrmCustomerMapper.xml"/>
    </mappers>
</configuration>